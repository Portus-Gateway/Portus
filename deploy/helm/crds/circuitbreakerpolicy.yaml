apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: circuitbreakerpolicies.portus-gateway.dev
  labels:
    gateway.networking.k8s.io/policy: direct
spec:
  group: portus-gateway.dev
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              required: [targetRef, circuitBreaker]
              properties:
                targetRef:
                  type: object
                  required: [group, kind, name]
                  properties:
                    group:
                      type: string
                    kind:
                      type: string
                      enum: [HTTPRoute, GRPCRoute, Service]
                    name:
                      type: string
                    sectionName:
                      type: string
                circuitBreaker:
                  type: object
                  required: [failureThreshold, successThreshold, timeoutSecs]
                  properties:
                    failureThreshold:
                      type: integer
                      minimum: 1
                    successThreshold:
                      type: integer
                      minimum: 1
                    timeoutSecs:
                      type: integer
                      minimum: 1
            status:
              type: object
              properties:
                conditions:
                  type: array
                  items:
                    type: object
                    properties:
                      type:
                        type: string
                      status:
                        type: string
                      reason:
                        type: string
                      message:
                        type: string
                      observedGeneration:
                        type: integer
                      lastTransitionTime:
                        type: string
                        format: date-time
  scope: Namespaced
  names:
    plural: circuitbreakerpolicies
    singular: circuitbreakerpolicy
    kind: CircuitBreakerPolicy
    shortNames: [cbp]
